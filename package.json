{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "preview": "turbo run preview",
    "lint": "turbo run lint",
    "format": "prettier --write .",
    "ui:add": "pnpm --filter @repo/ui ui:add",
    "turso": "pnpm --filter web turso",
    "studio": "pnpm --filter web studio",
    "generate": "pnpm --filter web generate",
    "migrate": "pnpm --filter web migrate",
    "push": "pnpm --filter web push",
    "introspect": "pnpm --filter web introspect"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "eslint": "^8.57.1",
    "prettier": "^3.4.2",
    "prettier-plugin-svelte": "^3.3.2",
    "turbo": "^1.13.4"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@9.2.0"
}
